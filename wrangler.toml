name = "amongcraft"
type = "javascript"  # Usamos JavaScript sin Node.js

account_id = "tu_account_id"  # Reemplaza con tu account_id de Cloudflare
workers_dev = true
compatibility_date = "2026-01-28"

[build]
command = "echo 'No build step required'"  # No necesitamos pasos de build

[[workers_dev]]
route = "https://tusitio.workers.dev/*"  # Ajusta la ruta de tu Worker si es necesario

[build.upload]
dir = "."  # Usamos la raíz del proyecto

[main]
script = "index.js"  # Aseguramos que 'index.js' es el entry point
