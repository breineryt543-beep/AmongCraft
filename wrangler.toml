name = "amongcraft"
type = "javascript"  # Usamos JavaScript sin Node.js

account_id = "e036c45e8b9ec093fde60a3d39e51d34"  # Reemplaza con tu account_id de Cloudflare
workers_dev = true
compatibility_date = "2026-01-28"

[build]
command = "echo 'No build step required'"  # No necesitamos pasos de build

[[workers_dev]]
route = "https://amongcraft.isaccdetemu.workers.dev"  # Ajusta la ruta de tu Worker si es necesario

[build.upload]
dir = "."  # Usamos la raíz del proyecto

[main]
script = "index.js"  # Aseguramos que 'index.js' es el entry point
